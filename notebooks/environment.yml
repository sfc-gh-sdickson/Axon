# Snowflake Notebook Environment Configuration
# This file specifies the Python packages required to run the Axon ML Models notebook
# Upload this file when creating/configuring your notebook in Snowflake
# Note: Snowflake automatically uses the latest compatible versions

name: axon_ml_models
channels:
  - snowflake
  - nodefaults
dependencies:
  - python=3.10
  - snowflake-snowpark-python
  - snowflake-ml-python
  - scikit-learn
  - xgboost
  - matplotlib
  - pandas
  - numpy

